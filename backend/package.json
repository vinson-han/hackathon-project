{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development nodemon --env-file=.env app.js ",
    "build:ui": "rm -rf dist && cd ../hackathon-project && npm run build && cp -r dist ../backend",
    "start": "npm run build:ui"
   
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.0"
  }
}
